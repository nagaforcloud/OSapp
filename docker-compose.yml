# docker-compose.yml
version: '3.8'

services:
  rag-app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./vector_db:/app/vector_db
    env_file:
      - .env
    restart: unless-stopped